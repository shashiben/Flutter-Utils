name: dev_utils
description: A collection of powerful Flutter utilities and extensions to streamline development, boost productivity, and simplify common tasks like UI enhancements, validation, and state management."
version: 0.0.3
homepage: https://github.com/shashiben/Flutter-Utils
environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  universal_io: ^2.2.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
