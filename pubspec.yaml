name: dev_utils
description: Utils to make developement faster.
version: 0.0.3
homepage: https://github.com/shashiben/Flutter-Utils
environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
