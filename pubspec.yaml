name: dev_utils
description: A collection of powerful Flutter utilities and extensions designed to speed up development, enhance productivity, and simplify common tasks.
version: 0.0.3
homepage: https://github.com/shashiben/Flutter-Utils
environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
